# :name, :string, :limit => 255
# :data, :text
layout:
  id: 1
  name: layout
  data: "<html><body><h1>This is the layout</h1>{{ content_for_layout }}</body></html>"
home:
  id: 2
  name: home
  data: "<p>home</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
index:
  id: 3
  name: index
  data: "<p>index</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
single:
  id: 4
  name: single
  data: |-
    <div class="article">
      <h3>{{ article.published_at | format_date: 'standard' }}</h3>
      <h2>{{ article.title }}</h2>
      {{ article.body }}
    </div>

    <h3>Comments</h3>
    <ul class="comments">
    {% for comment in comments %}
    <li id="comment_{{ comment.id }}">
      <p>{{ comment.body }}</p>
      <p>by {{ comment.author }} on {{ comment.created_at | format_date: 'short' }}</p>
    </li>
    {% endfor %}
    </ul>

    {% commentform %}
    <p>Name: {{ form.name }}</p>
    <p>Email: {{ form.email }}</p>
    <p>Url: {{ form.url }}</p>
    <p>
      {{ form.body }}<br />
      {{ form.submit }}
    </p>
    {% endcommentform %}

tag:
  id: 5
  name: tag
  data: "<p>tag</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
archive:
  id: 6
  name: archive
  data: "<p>archive</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
page:
  id: 7
  name: page
  data: "<p>page</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
author:
  id: 8
  name: author
  data: "<p>author</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
search:
  id: 9
  name: search
  data: "<p>search</p>{% for article in articles %}<h2>{{ article | link_to_article }}</h2><p>{{ article.body }}</p>{% endfor %}"
error:
  id: 10
  name: error
  data: "<p>error</p>"